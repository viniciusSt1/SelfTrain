import { Button, Text, View} from "react-native";
import styles from "../styles/globalStyles";

export default function EditarMedidas({navigation}){
    return (
        <View style = { styles.center }>
            <Text>Página para editar medidas do usuário</Text>
        </View>
    )
}