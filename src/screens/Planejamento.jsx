import { Button, Text, View} from "react-native";
import styles from "../styles/globalStyles";

export default function Planejamento({navigation}){
    return (
        <View style = { styles.center }>
            <Text>Pagina Planejamento</Text>
        </View>
    )
}